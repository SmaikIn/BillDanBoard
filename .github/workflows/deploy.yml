name: Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to Server
        run:
          sudo su - user -s /bin/bash -c && ${{ secrets.USER_PASSWORD }}
          cd && cd ../../var/www/ && ls -la

